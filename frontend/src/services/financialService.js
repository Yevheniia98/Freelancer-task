/**
 * Financial Service (JavaScript wrapper)
 * Re-exports from the TypeScript implementation for backward compatibility
 */

// Re-export the default export from the TypeScript implementation
export { default } from './financial.service.ts';

// Also export named exports if any component needs them
export * from './financial.service.ts';
